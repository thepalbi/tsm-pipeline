function id(
  x // candidateRep: (parameter x (member id (root https://www.npmjs.com/package/pkg5)))
) {
  return x; // candidateRep: rhs (return (member id (root https://www.npmjs.com/package/pkg5)))
}

export { id };

function id(
  x // candidateRep: (parameter x (member id (root https://www.npmjs.com/package/pkg2)))
) {
  return x; // candidateRep: rhs (return (member id (root https://www.npmjs.com/package/pkg2)))
}

module.exports.id = id;

function id(
  x // candidateRep: (parameter x (root https://www.npmjs.com/package/pkg6))
) {
  return x; // candidateRep: rhs (return (member default (root https://www.npmjs.com/package/pkg6)))
}

export default id;
